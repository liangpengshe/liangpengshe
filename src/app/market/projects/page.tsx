import { MarketContent } from '@/components/market/MarketContent'

/**
 * AI 智富项目库 - 独立路由
 * 默认 Tab：projects（对接匹配引擎 + 8 个项目方向）
 */
export default function MarketProjectsPage() {
  return <MarketContent defaultTab="projects" />
}
